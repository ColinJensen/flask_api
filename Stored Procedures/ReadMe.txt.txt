This creates stored procedures for the python scripts. Run 'generate_tables.py' first.
You will need to remove the two calls to stored procedures first

The first time you run these change all of the 'alter's to 'create'
You must also create the view for your data